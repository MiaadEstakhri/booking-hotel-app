export type HotelTypes = {
  picture_url: {
    filename: string;
  };
  xl_picture_url: string;
  smart_location: string;
  name: string;
  price: number;
  id: number;
  latitude?: number;
  longitude?: number;
  host_location?: string;
};
